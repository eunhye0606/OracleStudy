파일 자체가 어떤 기능을 하는 것이 아니다.
SQL 파일이 어디에 전달하지 않기에..........
주석 ( 공부하는 차원에서 ... 프로젝트가 아니자나 ......)


이미 쿼리문이 오라클에게 전달이 되고
오라클이 내부적으로 쿼리를 실행하면
그 쿼리 문이 사라져도 
그 테이블이 삭제되지 않는다.


COMMENT 달면 너무너무 좋아!

개발자 연봉 쌓이면 프리랜서로 돌릴 확률 높음.
프리랜서가 연봉 더 높음.

여기저기 프로젝트를 할 거니까
자바 주석처리
오라클 커멘트를 잘 다는게 좋다!
→ 그러면 같이 프로젝트하자고 찾아옴.

팀원을 배려하자!

자바에서 System.in
         Array.num
         『.』 → 안에~
오라클도 마찬가지!

자바에서도 한글명 변수는 가능하지만 절대 XX
오라클에서도 한글명 테이블 절대 XXX
(디렉토리명도 한글가능한데 경로따질때XXX)
	오라클 - 테이블명, 컬럼명
		길이 제한도 있음.
		반드시 영어로만!
		그래서 이름만 보고 테이블,칼럼이
		무슨일하는지 100% 알수없기에
		COMMENT활용꼭하기!


-- 코멘트명 변경은 그냥 쓰면 덮어 써진다.

주민번호 나중엔 데이터상황이 이렇게 될거니까 숫자타입을 
쓰면 안돼!
맨앞에 0 이 들어오는 구조 (휴대폰번호, 주민번호.. 등)
자바에서는 진법이 변화되는 구조때문에 문제
오라클에서는 데이터접두어 인식 기능 X 
오라클에서는 데이터 입력때 0이 탈락돼서 
자릿수가 부족해짐.
유니코드문자형은 한글로 딱 정해질때만 쓰고 보통 안씀!

헤더 와 바디
우리가 데이터 다루는건 바디영역!

포렌식에서 중요한건 파일의 내용물(바디)가 아니라
헤더가 중요함...
--> 문자열 고정형, 가변형 얘기하는중에 나옴..

VARCHAR2(13) → 이거 안에 뭐가 들어오는지 누군가가(헤더에서)
                매번 모니터링하고있어야함..

고정형 VS 가변형
안에 들어가는 데이터 변화 폭에 따라 다름.!


EMP 테이블의 층을 늘리는 ALTER

테이블안에서 컬럼의 순서는 의미가 없음
SELECT로 조회할때 순서를 바꾸면 되기때문에.

구조적으로 삭제 
→ ALTER DROP
--------------------------------------------------------------
--○ NULL 의 처리 (데이터 처리에서 중요!)

NULL은 일반적인 연산자로 산술연산 X 
물리적으로 존재 X 
논리적인 연산자가 필요!

COMM IS NOT NULL
NOT COMM IS NULL
--------------------------------------------------------------
SYSDATE() 매개변수가 필요없을 뿐 함수임.
NVL()도 함수! 매개변수필요!
